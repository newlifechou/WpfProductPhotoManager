<Window x:Class="WpfProductPhotoManager.PhotoList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfProductPhotoManager"
        mc:Ignorable="d"
        Title="查询列表" Height="450" Width="400" WindowStartupLocation="CenterScreen"
        WindowStyle="ToolWindow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <ListBox Style="{StaticResource MaterialDesignListBox}" Margin="2" Name="LstPhotoList"/>
        <Button Content="下载该列表所有文件" Name="BtnDownloadAllFiles" Style="{StaticResource MaterialDesignOutlinedDarkButton}"
                Grid.Row="1" Margin="2" Click="BtnDownloadAllFiles_Click"/>
    </Grid>
</Window>
